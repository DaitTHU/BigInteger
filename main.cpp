#include "bigMath.h"
using namespace std;

int main()
{
	/*
	for (unsigned n = 1; n <= 30; ++n)
		cout << n << "! = " << factorial(n) << endl;
	*/

	uInt m = factorial(10000);
	cout << uInt("1919810") / uInt("114514") << endl;
	return 0;
}